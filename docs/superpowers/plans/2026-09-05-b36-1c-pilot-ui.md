# Zero Dead-End Pilot UI Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development. Steps use checkboxes. This elaborates Task5 of the parent controlled-pilot plan; the parent ledger remains its execution owner.

**Goal:** Every visible controlled-pilot entry has a usable supported path.

**Architecture:** Add one server-authoritative optional-capability projection. Complete existing authenticated download, booking action, ticket readback and announcement flows with the current APIs and minimal additive contracts. Preserve account/payment and tenant security; no provider activation or new subsystem.

**Tech Stack:** Existing Express, PostgreSQL, React/Vite, Node test runner and Docker; no new dependencies.

**Spec:** docs/superpowers/specs/2026-09-05-b36-1c-pilot-ui-design.md. Spec wins; parent execution plan is docs/superpowers/plans/2026-09-05-b36-pilot-completion.md. Payment publication base: `15bf5b7`.

## Global constraints

- Request community is authoritative; B35F downloads remain Bearer-only and trusted-association bound.
- No new provider, SLA, assignment, booking cancellation role, amenity CRUD or scheduling subsystem.
- Manual proof remains the primary payment path; AI/MP/Twilio configuration is not fabricated.
- No secrets in health, logs, reports or rendered UI.
- Existing named upload volume and original migration checksums remain untouched.
- Fresh nonseeded QA identities, explicit Docker database, exact cleanup only.
- Preserve the currently running JWT_SECRET, INVITATION_TOKEN_SECRET and PUBLIC_APP_URL byte-for-byte across every QA rebuild. Read them only into process memory, never substitute test values or print them. Use the parent workspace rebuild-preserving-runtime.ps1 helper; missing .env does not mean missing runtime configuration.
- RED before behavior changes; actual rendered browser evidence for copy/UI, not source-grep assertions.
- A deliberate disabled-provider API503 is negative QA, not permitted normal-navigation failure. Normal UI must not invoke disabled integrations.
- New selected-record UI work must guard request/modal identity and per-operation busy ownership. Apply committed mutation responses before refresh; failed readback is separate from failed mutation, with safe disabled/retry state and no stale record actions. Deferred and post-commit refresh-failure component tests cover those paths.

## Preflight interface audit

| Task or pair | Producer/consumer checked | Resolution |
|---|---|---|
|5a|Pure capability booleans, health projection, controller early return and frontend default|One three-boolean shape; true means configured, not provider delivery verified. Optional disabled API probes are separate negative QA.|
|5b|Document rows/file_url and shared blob helper; booking mapping versus backend transitions|Reuse Task4c helper. Both UI and backend use pending/active/finished/cancelled; no new resident role action.|
|5c|Persisted ticket_replies versus missing detail route and optimistic UI|Add scoped detail GET before claiming follow-up complete; tests must exercise persisted two-actor readback and stale response protection.|
|5d|Public versus internal digital-token queries, dashboard readback, generated link logs|Public projection only; internal hashing unchanged. Resident dashboard currently is not cached, so no cache subsystem is needed. Visitor log change requires actual RED.|
|4b -> 5a|Committed payment success and detached optional notification helpers|Capability absence check precedes phone lookup; keep release-before-response/delivery boundaries, unknown-COMMIT handling and proof cleanup.|
|4c -> 5a|Expensas manual-primary UI and new provider visibility|Add optional capability branch without replacing proof action or restoring an unavailable provider button.|
|4c -> 5b|Authenticated canonical blob-download interface and durable named volume|Use the implemented signature unchanged; no raw anchor/query JWT, no duplicate download implementation.|
|5a -> 5b,5c|Layout capability fetch and independent module navigation|Fail-closed external features must not hide Documents, Amenities or Tickets or add auth/tenant state to capability cache.|
|5b -> 5c|No shared production files except readiness evidence|Sequential commits and separate reports; notification message changes stay in the owning controller.|
|5c -> 5d|Existing access navigation and metadata consumers|No new visitor portal; preserve guard presented-link/code workflow and repeat-use idempotence.|
|5a,5b,5c,5d -> acceptance|Role/scope/browser evidence and Docker fixture ownership|Only one runtime owner, preserve config/volumes, use fresh explicit-Docker fixtures and exact cleanup; final journey still runs after publication.|

### Task 5a: Server-authoritative optional capabilities

Files: new src/backend/config/capabilities.js and src/backend/test/pilotCapabilities.test.js; modify src/backend/server.js, src/backend/controllers/chatController.js, src/backend/controllers/paymentController.js, src/backend/services/whatsapp.js, src/backend/jobs/masterTicketQueue.js, src/backend/controllers/expenseController.js, src/backend/.env.example, existing src/backend/test/paymentController.test.js and src/backend/test/masterTicketQueue.test.js as needed. Frontend new src/frontend/src/services/capabilities.js and src/frontend/test/pilotCapabilities.test.js; modify src/frontend/src/components/Layout.jsx and src/frontend/src/pages/Expensas.jsx; src/frontend/src/components/WhatsAppButton.jsx only if its existing manual handoff needs minimal layout/role adjustment.

Interfaces: getCapabilities(env = process.env) returns exactly aiAssistant, mercadoPago, automaticWhatsApp booleans. Health adds capabilities without changing existing fields. createCapabilityService(client = api) exposes get(), one cached normalized result/promise; default capabilityService used by Layout and Expensas. Only true booleans enable UI; failed/malformed fetch is all false, reload permits a new attempt.

- [ ] Inspect actual optional variable consumers and committed example placeholders without emitting values. Write pure RED cases for missing/blank/partial/known-example config and complete synthetic config. Twilio requires SID/token/from; AI requires DEEPSEEK_API_KEY; MP requires MP_ACCESS_TOKEN. Do not reject valid sandbox tokens solely for a TEST prefix.
- [ ] Add real health projection and controller gate tests. Disabled chat/preference/webhook must return503 plus CAPABILITY_UNAVAILABLE before any database/provider work; route authentication/roles stay unchanged. Run backend `node --require ./test/setupEnvironment.js --test test/pilotCapabilities.test.js test/paymentController.test.js` and capture expected RED before implementation.
- [ ] Implement the pure config and health fields, then early unavailable-controller checks. Remove unused MP chat import. Configured mocked paths preserve existing behavior; no real provider calls.
- [ ] Add RED WhatsApp absence tests proving no phone lookup/task/enviando log when configuration incomplete. Implement services/whatsapp.js isConfigured and gate optional expense/queue work. Preserve core DB errors and committed manual-payment success on optional failure.
- [ ] Test the configured-AI/unconfigured-MP prompt sent to the mocked provider; it must recommend manual proof and not promise a missing MP button. Shared manual WhatsApp confirmation uses neutral approval copy, not automático. Do not refactor provider automation.
- [ ] Add frontend injectable-service RED tests for one shared request, exact boolean projection, malformed/failed response and false default before load. Run `node --test test/pilotCapabilities.test.js`, then implement the cache and capability-controlled Layout/Expensas rendering. Manual wa.me remains user-initiated for the existing admin/resident roles and independent of AI; guard remains excluded, with no automatic-delivery claim.
- [ ] Set optional provider examples blank; do not add optional provider Compose wiring or credentials.
- [ ] Run focused then full backend/frontend/build. Default Docker browser at desktop1366+/mobile390: no AI/MP dead controls or related calls, manual proof works, manual wa.me target retained when contact exists. Probe disabled APIs separately; exact QA cleanup. Commit `feat: gate unavailable pilot integrations`; fresh task review.

### Task 5b: Authenticated documents and explicit booking actions

Files: frontend src/pages/Documents.jsx, src/pages/Amenities.jsx, new src/utils/bookingTransitions.js and test/bookingTransitions.test.js; use Task4c src/services/protectedUploads.js rather than duplicating it. Existing src/services/bookings.js may expose an injectable factory if necessary for behavioral tests. Backend controllers/bookingController.js, models/Booking.js, new test/bookingLifecycle.test.js and existing b35cTenantIsolation tests. Notification.js/Hierarchy.js interfaces may gain optional client only if the actual call graph needs it; no unrelated refactor/migration.

Interfaces: booking transitions pending -> active|cancelled; active -> finished|cancelled; finished/cancelled -> none. Keep PUT /api/bookings/:id/status admin-only. Invalid payload400; invalid/stale state409; foreign id404. Existing create POST remains resident-only and preserves max_hours/advance_hours/deposit rules.

- [ ] RED Documents actual rendered missing-download and false-empty-on-load-error behavior; reuse existing frontend component harness/browser instead of adding a framework. Add safe filename/helper integration test if new logic needs it. Per-document authenticated Descargar PDF invokes Task4c helper with existing canonical stored path, safe local filename, row busy/error state, objectURL cleanup. No new documents service solely for indirection.
- [ ] Separate Documents loading/error/loaded-empty/data. Preserve admin create and read roles. Actual browser admin uploads PDF, admin/resident download identical bytes; foreign/guard/revoked denied; volume survives recreate. Defer shared live fixture run until remaining task code is green.
- [ ] Write pure booking transition RED cases covering all branches and self/terminal/jump rejection. Add backend controller/model expected-state/tenant/role/notification rollback regressions. Run `node --require ./test/setupEnvironment.js --test test/bookingLifecycle.test.js test/b35cTenantIsolation.test.js` backend and `node --test test/bookingTransitions.test.js` frontend before edits.
- [ ] Replace actions[0] with selected-event detail and explicit Spanish allowed buttons; terminal records show status without mutation actions. Keep admin-only controls, distinct Cancelar/Finalizar, per-action disabled/loading/errors, readback refresh. Resident can inspect its reservation state; no new cancellation permission.
- [ ] Use scoped expected-state update within a transaction shared with existing in-app notification. The update cannot leave committed status with a false notification-write failure response; failed transaction leaves both unchanged. Translate notification states without new channels.
- [ ] Reproduce invalid/nonfinite date input and existing SELECT-overlap/INSERT race before changing create. Validate finite dates before SQL. Use one amenity-row lock and same transaction client for overlap check/insert/notification on confirmed race. Preserve scoped amenity lookup and exact existing no-overlap rule; one competing request201 and one409. Maintain lock order for related status actions; no new scheduling subsystem.
- [ ] Verify with explicit Docker PostgreSQL two concurrent overlapping creates and concurrent status changes, notification-write rollback, valid/foreign IDs. Run full backend/frontend/build; actual browser both pending branches/both active branches/terminal/no resident admin controls, admin and resident notifications, desktop/mobile and load/action errors. Clean own rows/files exactly. Focused commits `feat: enable protected document downloads` and `fix: make booking actions explicit and atomic`; fresh full-task review.

### Task 5c: Truthful tickets/login and safe pilot setup

Files: src/frontend/src/pages/Tickets.jsx, Login.jsx; src/backend/controllers/ticketController.js and existing test/ticketController.test.js; docs/PILOT_READINESS.md. No migration/seed.js edit or non-QA DB cleanup.

Readback correction adds src/backend/models/Ticket.js, routes/tickets.js, frontend src/services/comunicacion.js and focused thread tests. Existing replies persist in ticket_replies but no GET consumes Ticket.getReplies; UI only appends local selected.replies.

- [ ] RED missing persisted thread and authorized own/foreign/deleted access. Add GET /api/tickets/:id after /my, admin/residente authentication, community-bound query and resident owner restriction; response existing ticket fields plus ordered replies. Foreign/absent/nonowned/deleted404, guard403. No broader mutation permission changes.
- [ ] RED actual detail load/close/selection race and hard-refresh loss. Add ticketService.get(id), existing timeline fresh detail loading/error/retry, server readback after reply/status, generation/closed-panel protection against stale completion. Two actors and hard refresh must both display persisted replies; no extra attachment/chat/SLA UI.

- [ ] Reproduce rendered disabled assignment/SLA tabs, fake notification checkbox and seed-shaped Login placeholders at current desktop/mobile. Functional RED test existing status notification Spanish label/recipient before controller copy change. Run backend `node --require ./test/setupEnvironment.js --test test/ticketController.test.js`; this existing test path is verified.
- [ ] Remove misleading/dead ticket tabs and dead active-tab logic only; preserve status/priority/category filters, reporter ownership, create/detail/reply/status. Urgent copy describes impact/priority with no response-time promise. Sin tomar becomes Estado inicial. Replace disabled notification checkbox with actual in-app admin-response/status behavior. No new SLA/assignment fields or filters.
- [ ] Spanish status notification labels preserve existing recipients/tenant checks. Neutral Login placeholders, email/current-password autocomplete and existing accessible labels; auth behavior unchanged.
- [ ] Document fresh trusted pilot provision and exact marker/ID QA cleanup. Keep SEED_DB:false; never run destructive seed.js or fuzzy-delete legacy communities/demo text. Unknown historical data remains untouched. Named upload volume is persistent, not a backup; no backup/trash subsystem.
- [ ] Full backend/frontend/build, actual fresh admin/resident ticket status/reply notification and working filters at1366+/390, truthful copy/neutral login/zero console errors, exact cleanup. Commit `fix: remove unsupported pilot UI promises`; fresh task review.

### Task 5d: Exact acceptance readback defects

Files: models/VisitorDigitalInvitation.js, models/Dashboard.js and announcement cache boundary only if reproduction requires it, focused digital-invitation/dashboard tests, src/frontend/src/pages/Anuncios.jsx and actual-component announcement lifecycle tests, src/frontend/nginx.conf plus a Docker behavioral sensitive-link log test if the suspected generated visitor-path leak reproduces. No new schema/dependency/token format/public visitor page.

- [ ] RED actual serialized admin list/create metadata/revoke includes token_hash. Replace public vdi.* projection with exact necessary schema/UI fields, preserving raw token only as deliberate creation response and existing short hint. Verify no full token/hash in later metadata, JWT rotation compatibility, revocation, foreign roles, same-visit idempotency. Focused backend `node --require ./test/setupEnvironment.js --test test/visitorDigitalInvitation.test.js test/accessInvitationController.test.js`; both existing test paths are verified.
- [ ] RED fresh same-community create/delete announcement then resident dashboard still returns deleted row. Filter deleted_at and invalidate only the existing affected cache if necessary. Test foreign isolation and new rejected payment counts remain correct; real PostgreSQL QA only exact own fixtures.
- [ ] RED actual Anuncios component load rejection while create form is closed: visible error/retry, not false empty; failed delete/mark-read must be handled; pending creation/action must not duplicate. Implement the smallest loading/error/empty/data and per-operation feedback guards. Apply committed create/delete/read result before refresh and keep a distinct retry warning for failed readback. Deferred list responses cannot resurrect a just-deleted/read row. Preserve existing text-only API, role and confirmation behavior; no additional announcement feature.
- [ ] Isolated Docker RED for synthetic /invitacion/<token> casing/encoding access/error logs and subsequent asset Referer. If confirmed, protect that credential route and response referrer without altering guard's existing presented-link/code workflow. Verify final SPA serving/internal fallback and error responses, not source-only rules. No synthetic credential output or broad visitor portal implementation.
- [ ] GREEN focused/full affected tests/build/Docker/same-foreign live evidence, exact cleanup, focused commits by responsibility, report task-5d-report.md and fresh task review. Only then run the complete block gate below.

## Concrete contract checks for the task RED steps

Task5a defines the pure capability export and the corresponding frontend service. Start with these literal expectations in the named test files; add the required controller/mocked-outbound and actual-rendered cases before editing their production consumers:

```js
const assert = require('node:assert/strict');
const { getCapabilities } = require('../config/capabilities');
assert.deepEqual(getCapabilities({}), {
  aiAssistant: false, mercadoPago: false, automaticWhatsApp: false,
});
assert.equal(getCapabilities({ DEEPSEEK_API_KEY: ' ' }).aiAssistant, false);
assert.equal(getCapabilities({ TWILIO_ACCOUNT_SID: 'configured-test-sid' }).automaticWhatsApp, false);
```

Task5b defines `getBookingActions(status)` in `src/frontend/src/utils/bookingTransitions.js`, returning allowed target-enum strings, and uses the same transitions in backend validation. Put all four rows plus unknown/self/jump checks in `test/bookingTransitions.test.js`:

```js
import assert from 'node:assert/strict';
import { getBookingActions } from '../src/utils/bookingTransitions.js';
assert.deepEqual(getBookingActions('pending'), ['active', 'cancelled']);
assert.deepEqual(getBookingActions('active'), ['finished', 'cancelled']);
assert.deepEqual(getBookingActions('finished'), []);
assert.deepEqual(getBookingActions('cancelled'), []);
assert.deepEqual(getBookingActions('unknown'), []);
```

The existing download signature is `downloadProtectedUpload(fileUrl, fileName, { client, browser })`. Documents calls it with the returned canonical `file_url` and safe local PDF name. Tests inject the existing client/browser boundary and assert the root-relative Bearer request, one click, revoked object URL and visible failure; they must not replace the component's asynchronous handlers with duplicated logic.

Task5c defines `ticketService.get(id)` as `api.get('/tickets/' + id)`; backend GET returns the existing ticket fields plus `replies` ordered by creation. Actual component tests use the established bundle harness (as in frontend `test/manualPayment.test.js`) with deferred service promises. Sequence: selectA, selectB, settleB, settleA; assertB remains selected. For saved reply/status plus failed refresh, assert the saved state, warning and retry, not a second submit. Mount a new component and feed the persisted GET response to prove the thread is not only local state.

Task5d tests serialized public metadata, not just SQL text. With a synthetic invitation generated only in test memory, assert list/create.invitation/revoke objects have no own `token_hash` or raw-token property; assert the intentional top-level creation token still exists, and its hash remains stored. The Docker log test holds the synthetic token only in memory, asserts absence in complete access/error captures, and reports fixed pass/fail messages without credential-bearing assertion diffs.

Each task runs its listed focused command after adding tests: expected failure must identify the absent contract, not a harness/runtime error. Implement the minimum, rerun the same focused command to zero failures, then the whole gate. Refactor only while green, and keep each independently testable responsibility in its own focused commit.

## Whole-block gate and publication

- [ ] Review the complete payment-published-base..Task5HEAD diff with an independent whole-block reviewer. Resolve load-bearing findings with RED/GREEN and scoped re-review; record rulings and evidence in parent ledger.
- [ ] Run backend `npm test` with dedicated disposable Redis instance (never appRedis because suite clears rl:*), frontend `node --test test/*.test.js` and `npm run build`, `git diff --check`. The parent workspace verify-regression-gate.ps1 owns exact disposable-Redis cleanup. Rebuild backend/frontend through rebuild-preserving-runtime.ps1 -Build, with outer PowerShell ErrorActionPreference=Stop; helper pins the existing comunidad-app project and verifies unchanged protected config. Check health and actual served asset. Never render Compose config or dump container environment.
- [ ] Record real results in docs/PILOT_READINESS.md. Actual provider SMTP remains external NO VALIDADO only, not a reason to skip other gates.
- [ ] Fresh fetch, clean behind0 branch, fast-forward main without rewriting history, test merged main, normal push origin main and verify equality/clean. No worker pushes, no force. Continue parent Task6 acceptance; do not invent additional features.
